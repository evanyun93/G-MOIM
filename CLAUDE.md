# G-MOIM

건담 프라모델 커뮤니티 플랫폼. 1인 개발.

킷 위키(공용 데이터) + 개인 작례 아카이브(블로그) + 커뮤니티(갤)를 한 사이트에 결합한다.
기존 플랫폼은 실시간 소통과 개인 아카이브 중 하나만 갖고 있고 구조상 나머지를 붙일 수 없다는 것이 출발점이다.

**MVP 범위** — 킷 위키 + 개인 블로그 + 계정. 커뮤니티 게시판은 제외.

**현재 단계** — 설계 진행 중. 기술 스택 미정. 애플리케이션 코드 없음.

---

## 시작할 때

**`docs/STATUS.md`를 먼저 읽는다.** 설계 트리, 확정 결정 목록, 지금 어느 노드를 작업 중인지가 거기 있다. 짧다.

설계 문서 전체(`docs/design/`)는 필요할 때만 연다. 2만 자가 넘는다.

---

## 문서를 언제 읽나

작업 전에 해당 문서를 먼저 읽는다. **전체를 다 읽지 말고 필요한 것만.**

| 작업 | 읽을 것 |
|---|---|
| 현재 진행 상황, 무엇이 정해졌나 | `docs/STATUS.md` |
| 데이터 모델, 스키마, 마이그레이션 | `docs/design/` 최신 v 파일 3장 |
| 페이지 라우팅, 렌더링 방식 | 같은 파일 부록 F + STATUS의 결정 #13 |
| 이미지 업로드, 스토리지 | 같은 파일 부록 C + `docs/infra/인프라확장플랜*` |
| 인증, 계정 | 아래 절대 제약 + `docs/infra/벤더선택지*` |
| 킷 데이터 처리 | `docs/tasks/` 최신 지시서 |
| 배포, 벤더 선택 | `docs/infra/` |

`docs/design/`에는 버전 파일이 쌓인다. **v 번호가 가장 큰 것이 최신이다.** 구 버전은 이력이므로 참조하지도, 수정하지도 않는다.

`docs/tasks/`도 마찬가지다. `_v2` 같은 접미가 붙은 것이 현행이다.

---

## STATUS 갱신 규칙

설계 결정이 나거나 노드 상태가 바뀌면 **`docs/STATUS.md`부터 갱신한다.**

- 결정 표에 행을 추가하고 `반영` 열을 `미반영`으로 둔다
- 트리 노드 상태를 함께 바꾼다
- 설계 문서 버전(`v3`, `v4`...)은 결정이 5건 이상 쌓이거나 구조가 바뀔 때만 올린다
- STATUS는 짧게 유지한다. 근거, 논의 과정, 대안 검토는 설계 문서로 보낸다

코드가 특정 결정을 구현하는 것이라면 커밋 본문에 `결정 #N`을 남긴다.

---

## 절대 제약

설계에서 확정된 것들이다. **구현할 때 빠뜨리기 쉬우니 반드시 지킨다.**

### 이미지

- 업로드 시점에 **EXIF 메타데이터를 제거**한다. 옵션으로 두지 않는다.
  건프라 사진은 집에서 촬영되어 GPS 좌표가 박혀 있다. 법적 요건이자 사고 방지다
- 원본을 서빙 경로에 두지 않는다. 업로드 시 장변 2048px 리사이즈 + WebP/AVIF 변환
- 스토리지는 egress 무료인 것만. **S3 호환 API만 사용하고 벤더 고유 SDK를 쓰지 않는다**
- 이미지 URL은 불변. 콘텐츠 해시 기반 키. 사이즈 변형은 유한한 프리셋으로 고정

### URL과 렌더링

- URL은 ID가 아니라 슬러그. `/kit/mg-sazabi-ver-ka`, `/unit/rx-78-2`
- 슬러그 변경 이력 테이블을 유지해 구 URL 리다이렉트를 보장한다
- 페이지 성격별로 렌더링 방식이 다르다 (결정 #13)

  | 페이지 | 방식 |
  |---|---|
  | 킷, 기체 | SSG |
  | 작례 | SSG 또는 ISR |
  | 홈 피드 | SSR |
  | 글쓰기, 관리 | CSR |

  전면 SSR로 만들지 않는다. Cloudflare Workers 무료 티어가 일 10만 요청이라 금방 고갈된다

- 공개 전까지 전체 noindex. 작례 0개 킷 페이지는 색인 제외 (`is_indexable` 계산 컬럼, 하드코딩 금지)

### 인증

- 벤더 인증을 쓰더라도 **자체 `users` 테이블을 두고 벤더 ID는 외래키로만 참조**한다
- 다른 테이블이 벤더의 유저 ID를 직접 참조하지 않게 한다. 인증이 최대 잠금 요소다

### 데이터

- 작례는 유저 소유, 킷 DB는 공용 위키. `post_kits`는 소유가 아니라 **참조**다
- 애매한 관계는 M:N으로 간다. 조인 테이블 비용은 사실상 0이다
- 등급은 `grade_family` 단일 계층. 작품 접미어(HGUC의 UC)를 등급에 넣지 않는다. 그건 `series`의 몫
- 일별 스냅샷 집계 테이블(`daily_metrics`)은 소급 집계가 불가능하므로 초기부터 수집한다

### 외부 데이터

- 사실 필드만 수집한다. 위키 서술 텍스트는 라이선스 전염성이 있어 가져오지 않는다
- 제조사 이미지를 저장하지 않는다. 킷 대표 이미지는 유저 작례로 채운다
- 나무위키는 비영리 조건이라 소스로 쓰지 않는다

---

## 개발 일지

`docs/journal/{YYYY-MM-DD}.md`에 기록한다. 이 프로젝트는 시행착오 기록을 책으로 쓸 목적이 있다.

**언제 쓰나** — 판단이 갈렸을 때만. 매일 쓰지 않는다. 진척 보고는 커밋 로그가 대신한다.
구현 중 설계와 다르게 가야 했거나, 두 방법 중 하나를 골랐거나, 예상과 다른 제약을 만났을 때.

**형식**

```markdown
## #N. <제목>

**상황** — 무엇을 정해야 했나
**선택** — 무엇을 골랐나
**당시 근거** — 미화 금지. 나중에 부끄러운 이유여도 그대로
**반대 의견** — 안 고른 쪽의 논리. 없었으면 "없음"
**미판정 질문** — 지금 확신 못 하는 것. 이 일지의 핵심
```

미판정 질문에 나중에 `→ 판정:`을 이어 붙인다. 지우지 않는다. 틀린 예측일수록 가치가 있다.

**한 건 5분을 넘기지 않는다.** 기록 부담이 개발을 멈추게 하는 것이 최악이다.

파일이 이미 있으면 이어 쓰고, 같은 날 두 번째 세션이면 `_2회차` 접미를 붙인다.

---

## 커밋

```
feat:     기능 추가
fix:      버그 수정
docs:     문서
chore:    설정, 구조
data:     킷 카탈로그 등 데이터
refactor: 동작 변경 없는 정리
```

브랜치 — `main`은 검증 완료분만. 킷 카탈로그 수집은 `data/kit-db`에서 작업 후 머지.

---

## 이 프로젝트에서 반복된 실패 패턴

실제로 발생한 것들이다. 판단할 때 경계한다.

- **근거가 앞의 결정과 모순되는데 각각은 그럴듯해서 통과된다.** 새 근거를 댈 때 기존 결정과 충돌하는지 먼저 확인한다
- **후행 지표를 초기 조건으로 상정한다.** "검색 유입으로 사람이 온다" 같은 것. 유저 0명일 때도 성립하는지 묻는다
- **시점이 다른 조건을 하나로 묶는다.** 배포 시점과 공개 시점의 조건은 다르다
- **유저에게 단계를 하나 더 요구하는 방향으로 해결한다.** 강제하기 전에 그 수고를 없앨 방법을 찾는다
- **스키마에서 뺀 정보는 옆 필드로 새어 들어온다.** 재판 이력을 뺐더니 초판 데이터에 재판 품번이 섞였다
- **찾지 못한 것을 "없음"으로 보고한다.** 미확인과 부재를 구분한다

---

## 하지 말 것

- `docs/design/` 구 버전 파일을 수정하지 않는다. 이력이다
- 설계 결정을 코드에서 임의로 뒤집지 않는다. 문제가 있으면 먼저 말한다
- `_솔로개발_템플릿` 관련 내용을 이 레포에 넣지 않는다. 프로젝트 무관 자료다
- 확인하지 못한 값을 그럴듯하게 채우지 않는다. 비우고 표시한다
- `data/kits/`의 1·2차 수집분을 기준 데이터로 쓰지 않는다. 구 스키마이며 대조용이다
