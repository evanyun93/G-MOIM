# 2026-09-14

## #13. "목록은 살린다"를 하루 만에 철회했다 — Antigravity 1차 결과물 전면 반려

**상황**
Antigravity가 MG 목록 게이트에서 멈추지 않고 MG CSV, RG, PG까지 진행했다(627 / 226 / 26행). 어제 1차 점검에서는 "목록은 살리고 CSV만 다시"를 권장했다. 오늘 수치로 다시 세어보고 판단을 정해야 했다.

**선택**
전부 반려. 파일은 `_rejected_2026-09-13/`으로 옮겨 누락 대조에만 쓰고, MG 목록부터 01b 지시서로 재작업. RG·PG는 MG 통과 후.

**당시 근거**
- MG 제품명에 일본어 위키백과 서술 문장 77행. CC BY-SA 서술 혼입이라 절대 제약 위반
- 품번·시리즈·형식번호가 세 등급 전 행 공란, 전 행 `confidence=low`, `units.csv` 0행. 행 수만 맞춰놓고 "1:1 완벽 일치, 누락 없음"이라 보고
- 목록을 살릴 수 없는 이유 — 달롱넷·위키백과·fandom 표기가 섞여 `RX-78 v1.0`, `Zaku II 샤아전용`, `ガトー専用ゲルググ`가 한 목록에 있다. 기준 표기가 없으니 중복인지 판정할 방법이 없다. 어제는 행 수와 커버리지 표만 보고 목록이 쓸 만하다고 봤다
- 재작업 방식은 **공식 라인업 하나를 뼈대로, `name_ja` 공식 표기를 중복 판정 키로.** 여러 소스를 동시에 섞은 것이 뒤섞임의 원인이라 판단

**반대 의견**
627종이라는 물량은 옛 358종보다 크고, 한정판 전수 조사는 실제로 일부 해냈다. 버리면 시간 손실이 크다. 표기만 사후 정규화하면 된다. → 표기 정규화 자체가 공식 표기를 다시 찾아야 하는 작업이라 새로 만드는 것과 비용이 비슷하다. 옛 목록은 누락 대조에 쓰니 물량 정보는 잃지 않는다.

**미판정 질문**
- 01a에 "멈춤"을 표로, 굵게, 두 번 썼는데 세 번 넘었다. 01b에 "넘으면 안 보고 반려"를 적었지만 **문서로 게이트를 지키게 할 수 있나.** 한 번에 한 단계만 담긴 지시를 주는 게 맞는 방식일 수 있다
- 2018년 이후 MG 일반 유통이 연 1~3종으로 급감한 것이 실제인지, 달롱넷 목록이 끊긴 구간을 "적다"로 쓴 것인지. 재작업 결과로 판정된다
- 공식 사이트(bandai-hobby.net)가 과거 제품까지 빠짐없이 싣고 있나. 뼈대 소스가 틀리면 01b 방식 전체가 흔들린다
- "자체 점검은 숫자로만 보고"가 거짓 보고를 막는가, 숫자까지 지어내는가

---

## #14. 스타일링은 Tailwind v4, 색은 CSS 변수 토큰으로만

**상황**
4.1.5. 후보는 Tailwind v4, Astro 스코프 CSS + CSS 변수(도구 없음), UnoCSS.

**선택**
결정 #27. Tailwind v4. 색·간격·글꼴은 `src/styles/`의 CSS 변수 토큰으로만 정의하고 컴포넌트는 토큰 클래스만 쓴다. 글 본문은 typography 플러그인.

**당시 근거**
코드를 쓰는 AI가 둘이다(메인 세션, Antigravity의 킷 페이지). 클래스 이름을 각자 짓게 두면 스타일이 갈라진다. Tailwind는 AI가 가장 일관되게 쓰는 문법이다. 블로그 스킨(1.1.5)은 컴포넌트가 토큰만 참조하면 변수 교체로 끝난다.

**반대 의견**
스코프 CSS는 의존성이 0이고 이식성이 가장 높다. Tailwind는 마크업이 길어지고 빌드 도구가 하나 는다. → CSS는 벤더 잠금이 아니라 빌드 도구라 #19 위험이 작다고 봤다. 토큰이 CSS 변수라 Tailwind를 빼도 토큰은 남는다.

**미판정 질문**
- "색 값 직접 쓰기 금지"를 AI가 실제로 지키나. `bg-[#...]` 임의값은 Tailwind가 막지 않는다. 린트로 강제해야 할 수 있다
- 유저가 쓴 글 본문(`prose`)과 사이트 UI 토큰이 스킨 교체 시 같이 바뀌나, 따로 노나

---

## #15. 인증은 Better Auth 직접 운영, 로그인은 소셜만

**상황**
4.6. 후보는 Better Auth 직접 운영, Neon 관리형 Better Auth, Clerk, Supabase Auth. 로그인 수단은 소셜 전용 / 소셜 + 이메일 일회용 코드 / 소셜 + 비밀번호.

**선택**
결정 #28~#30. Better Auth를 우리 Postgres에 붙여 직접 운영. MVP는 카카오·네이버·구글 소셜 전용. 캐시되는 HTML에 로그인 상태를 넣지 않는다.

**당시 근거**
- 조사하다 **무료 Workers에서 비밀번호 해시가 CPU 10ms에 걸린다**는 걸 알았다(Better Auth 이슈 #8860). 비밀번호를 넣으려면 실측부터 해야 한다
- 메일 발송은 Cloudflare 것은 유료 플랜 전용, Resend 무료는 하루 100통 상한. 메일 폭탄 한 번이면 그날 가입이 막힌다. 소셜 전용이면 이 비용 벡터가 통째로 사라진다
- 과거에 DB 벤더에서 데이터를 잃었다(#19). 유저 테이블이 남의 서버에 있는 Clerk는 이 원칙과 정면으로 부딪힌다
- 한국 커뮤니티에 네이버 로그인은 사실상 필수인데 Supabase는 미지원, Clerk는 연결이 어려울 수 있다

**반대 의견**
Clerk는 로그인 UI·보안 운영·봇 방어를 통째로 맡아준다. 1인 개발자가 인증 보안 패치를 직접 따라가는 건 부담이다. 소셜 전용은 소셜 계정을 잃은 유저를 구제할 방법이 없다. → 소셜 여러 개 연결로 완화. 보안 패치는 라이브러리 버전 업데이트 수준이라 감당 가능하다고 봤다.

**미판정 질문**
- 카카오 비즈 앱(이메일 동의항목) 심사가 개인 개발자에게 실제로 통과되나. 안 되면 카카오 유저는 이메일 없이 산다
- 세션 조회를 쿠키 캐시로 줄이는 설정이 Workers + Hyperdrive 조합에서 제대로 동작하나. 요청마다 쿼리가 나가면 일 10만 한도를 작례 조회와 나눠 쓴다
- 소셜 전용이 가입 전환율을 떨어뜨리나, 오히려 올리나. 모델러 커뮤니티에서 "카카오로 시작하기"에 거부감이 있나
- 비밀번호 로그인 요구가 나오면, 그때 `node:crypto` scrypt가 10ms 안에 드는가

---

## #16. 에디터·이미지 세부 — Tiptap JSON, Safari는 JPEG, HEIC는 거부, 9GB 자동 차단

**상황**
1.1.3. 본문 저장 형식, Safari가 WebP를 못 만드는 문제, HEIC, 쿼터 수치, 게시 상태를 정해야 했다.

**선택**
결정 #31~#34. Tiptap + JSON 저장. 캔버스 WebP, 안 되면 JPEG. 못 여는 HEIC는 거부하고 건수를 센다. 프리셋 2048/1024/400, 신규 일 50장·300MB / 일반 일 200장·2GB, 전체 9GB에서 업로드 자동 중지. 게시 상태는 초안·공개·비공개 + 30일 휴지통.

**당시 근거**
- HTML 저장은 XSS 정화가 필요하고 Worker에서 DOM 흉내 라이브러리가 CPU를 쓴다. JSON은 허용 노드만 변환하면 정화가 필요 없고, 본문에서 사진 ID를 뽑기도 쉽다
- Safari는 WebP 요청에 에러 없이 PNG를 준다. 라이브러리(WASM)로 통일하면 폰에서 수십 장 변환이 느려진다. 저장량 1.3배를 감수했다
- 아이폰에서 바로 올리면 iOS가 JPEG로 바꿔준다. 걸리는 건 윈도우에 HEIC 파일을 옮긴 경우 정도라고 봤다
- **9GB 자동 차단은 R2 무료 초과분이 과금되기 때문.** 기존 차단 스위치는 재배포가 필요하고 예산 알림은 다음 날 온다. DB 합계로 즉시 막는 게 유일하게 "청구서"를 원천 차단한다

**반대 의견**
- HEIC 거부는 CLAUDE.md의 실패 패턴 "유저에게 단계를 하나 더 요구"에 그대로 해당한다. → 인정. 거부 건수를 세서 많으면 변환 라이브러리를 붙인다
- 9GB 차단은 공격이 아니라 **정상 성장**에도 업로드를 멈춘다. 서비스가 잘될 때 막히는 장치다. → 멈추는 게 과금보다 낫다. 인프라확장플랜 트리거(8GB)에서 유료 전환을 먼저 판단한다

**미판정 질문**
- 사진 한 장 변형본 합계 0.7MB(WebP)·1MB(JPEG)는 추정이다. 도색 디테일이 많은 건프라 사진은 WebP 압축 효율이 떨어질 수 있다
- 신규 계정 일 50장이 "첫날 제작기 몰아 올리기"를 막나. 네이버 블로그에서 옮겨오는 모델러는 첫날에 수백 장을 올린다
- 비공개 글의 사진 URL이 비밀이 아니라는 안내를 유저가 이해하나, 문제로 받아들이나
- Tiptap JSON 스키마가 버전업으로 바뀌면 저장된 본문이 깨지나. 본문에 스키마 버전을 넣어야 하나

---

## #17. v3로 모으다가 근거 없는 결정 5건과 결정 간 긴장 7건이 나왔다

**상황**
결정 #11~#34를 설계 문서 v3 한 곳에 모았다. 결정마다 근거를 옮겨 적어야 했다.

**선택**
근거 원문이 없는 결정(#11 #12 #14 #21 #34 일부)은 이유를 추정해 채우지 않고 "원문 미기록"으로 표시했다. 결정끼리 맞춰보다 드러난 문제는 고치지 않고 v3 5장에 모았다. 설계 트리는 STATUS가 원본이라 v3에서 뺐다.

**당시 근거**
- #11~#16은 레포 이관 전 결정이라 STATUS 한 줄만 남았다. 이전 대화 기록을 검색해도 비교 근거가 없었다. 초안에 추론을 근거처럼 쓴 곳이 세 군데 있었고 다시 읽다 지웠다
- 가장 큰 긴장은 **이미지 URL 불변·긴 캐시 ↔ 블라인드·임시조치.** 결정을 하나씩 볼 때는 각각 맞았다. 신고 대응을 한 줄로 이어 그려보고서야 "블라인드해도 URL로 열린다"가 보였다
- 개인화를 island로 뺀 #30이 로그인 유저 조회당 Worker 요청을 둘로 만든다. #30을 정할 때 캐시 사고만 봤고 요청 한도(#18)와 같이 보지 않았다

**반대 의견**
v3에서 긴장 항목을 바로 결정으로 풀면 문서가 한 번에 완결된다. → 각 항목이 속한 노드(2.2 신고, 1.1.4 이관, 백업 위치)가 아직 열리지 않아 지금 풀면 근거 없는 결정이 또 생긴다.

**미판정 질문**
- 근거 없이 요약만 남은 결정을 그대로 두는 게 맞나. 나중에 뒤집을 때 "왜 그랬는지 모르니 그냥 바꾼다"가 되지 않나
- 결정이 30건을 넘으니 한 건씩 정할 때 전체와의 충돌을 못 본다. 결정 때마다 "이 결정이 요청 수·쿼리 수·삭제 경로를 바꾸나"를 묻는 체크가 필요한가
- 5장의 12건 중 공개 전에 반드시 풀어야 하는 것은 몇 건인가. 7.1 로드맵에서 판정

---

## #18. 타겟을 "다"라고 답했고, 커뮤니티 유저만 2순위로 뺐다

**상황**
0.3.2. 결정 34건에 깔린 유저 가정을 표로 꺼낸 뒤 1순위 타겟을 물었다. 작례 모델러와 입문·수집 유저 중 하나를 고르는 질문이었다.

**선택**
결정 #35. 답은 "모델러, 입문 유저, 정보공유, 커뮤니티를 즐기는 사람" 전부였다. 그중 커뮤니티 유저는 MVP(#11, 갤 제외)가 붙잡을 수단이 없어 2순위로 뺐다. 모델러 활동처가 카페·유튜브라는 답에서 유튜브 임베드(#36)가 나왔다.

**당시 근거**
- 전부를 타겟으로 두면 기능 우선순위 판단이 매번 갈린다. 다만 셋은 MVP 기능이 이미 각자 대응되고 있어 억지로 하나로 줄이지 않았다
- 커뮤니티 유저를 MVP 타겟에 두면 #11을 다시 열어야 한다. 일지 예측 "MVP를 크게 잡는 게 가장 큰 위험"이 있다
- **관찰된 활동처가 네이버 블로그가 아니었다.** 설계 문서 1장 차별점 표는 디시와 네이버 블로그만 비교하고 있었다. 카페는 흐름과 축적을 이미 한 곳에 갖고 있다. 차별점 전제가 비교 대상을 잘못 고른 채 34건이 쌓였을 수 있다

**반대 의견**
타겟을 하나로 좁혀야 초기 기능과 메시지가 선명해진다. 셋을 두면 성공 기준도 셋이 된다. → 로그인 유형(작성)과 비로그인 유형(조회)으로 지표를 둘로 묶어 0.4에서 다룬다.

**미판정 질문**
- 대형 카페 모델러가 카페를 두고 따로 올릴 곳을 원하나. 카페에 올리고 링크만 거는 것이 실제 행동이면 블로그 설계 전체가 과하다
- 1.1.4 네이버 블로그 이관을 보류해둔 것이 결과적으로 맞았나. 활동처가 블로그가 아니라면 애초에 이관 대상이 적다
- 운영자 한 명의 관찰로 타겟을 정했다. 5.2에서 모델러 몇 명과 이야기해보면 뒤집히나

---

## #19. 성공 기준을 세우려다 조회 수를 셀 수 없다는 걸 알았다

**상황**
0.4. 타겟이 셋(#35)이라 지표도 여럿이 필요했다. 초대 공개 판정은 모델러가 반응을 받는지에 달렸는데 MVP에 반응 수단이 없었다.

**선택**
결정 #37~#39. 공개 관문 / 초대 공개 8주 판정 / 자연 유입 관찰 / 운영자 본인 사용으로 기준을 나눴다. MVP에 1단 댓글을 넣었다. 조회 수는 Cloudflare Web Analytics 수치를 매일 DB에 옮긴다.

**당시 근거**
- **킷·기체는 정적 파일, 작례는 캐시 적중 시 Worker 코드가 안 돈다.** #13·#18로 요청 한도를 아낀 설계가 그대로 "우리 코드는 조회를 못 본다"가 됐다. 지표를 정하려고 하기 전까지 아무도 몰랐다
- 자체 비콘은 조회마다 Worker 요청을 하나 더 쓴다. 아낀 한도를 측정에 도로 쓰는 셈이다
- Web Analytics 보존 기간이 요금제별로 공개돼 있지 않아, 벤더에만 두면 #14의 "소급 불가"에 걸린다
- 초대 모델러 판정을 글 1개가 아니라 2개로 한 것은, 첫 글은 초대에 대한 예의로 올릴 수 있어서다
- 댓글은 #11(갤 제외)을 넓히는 것이지만, 반응 없는 곳에 모델러가 계속 올릴 거라는 가정은 판정 자체를 무의미하게 만든다

**반대 의견**
- 댓글을 넣으면 쓰기 API·쿼터·신고·검수가 한 벌 는다. 일지 예측 "MVP를 크게 잡는 게 가장 큰 위험"에 정면으로 걸린다
- 벤더 분석 도구는 #14 "자체 구축"과 어긋난다. → 코드가 볼 수 있는 것은 자체 집계, 볼 수 없는 조회만 벤더에서 받아 우리 테이블에 쌓는 것으로 선을 그었다

**미판정 질문**
- 분모가 5~6명인 "절반" 기준이 판정으로 의미가 있나. 한두 명의 개인 사정으로 갈린다
- 초대 공개 단계에서 댓글을 다는 사람이 초대된 모델러끼리뿐이면, 댓글은 반응 수단이 아니라 품앗이가 된다
- 설계가 한도를 아끼는 방향으로 갈 때마다 관측 가능성을 잃고 있는가. 캐시·정적화 결정 때 "이러면 무엇을 못 보게 되나"를 물어야 했다

---

## #20. 로드맵을 짜다 검색 설계가 DB와 안 맞는 걸 발견했다

**상황**
7.1. 마일스톤을 짜면서 M1(킷 데이터)의 검색을 적으려고 4.5 "Postgres + pg_bigm"을 확인했다.

**선택**
결정 #40·#41. Neon 확장 목록에 pg_bigm이 없어 pg_trgm으로 바꿨다. 구현 순서는 M0 기반 → M1 킷 데이터 → M2 계정 → M3 블로그 → M4 공개 준비. 공개 관문의 운영자 작례는 개수 대신 "블로그 완성 후 2달간 G-MOIM에만 올림"으로 했다.

**당시 근거**
- pg_bigm은 설계 초기(스택 선정 전)에 적힌 방향이었고, 이후 Neon을 고를 때 대조하지 않았다. **"찾지 못한 것을 없음으로"의 반대 — 확인하지 않은 것을 "된다"로 둔 것**
- MVP 검색 대상은 킷·기체·별칭이라 규모가 작다. 2글자 한국어 검색은 인덱스 없이 훑어도 된다고 봤다. 실측은 M1 완료 조건
- 운영자 작례 개수를 묻자 "공개 관문 A가 뭔지 모르겠다"는 답이 왔다. 성공기준 문서에서 만든 용어를 설명 없이 질문에 썼다. 풀어 설명하니 개수보다 "실제로 쓰는가"를 골랐다. 상시 기준(운영자 본인 사용)과 같은 것을 재는 쪽이다
- 공개 전 필수 검토 항목은 넷(① 이미지 내리기, ② 정적 페이지 지연, ⑤ 신고, ⑦ 백업 위치)으로 좁혔다. 나머지는 실측이거나 문서 정리다

**반대 의견**
pg_bigm을 지원하는 DB(RDS 등)로 바꾸면 한국어 2글자 검색 품질이 확실하다. → 무료 한도·Hyperdrive 조합을 다시 조사해야 하고, 검색 대상이 작은 MVP에서 얻는 차이가 작다.

**미판정 질문**
- 벤더를 고를 때 그 전에 적힌 설계(확장·기능)를 전부 대조하는 체크리스트가 필요한가. 이번 건은 결정 34건째 정리에서도 못 잡았고 로드맵 작성 중에 잡혔다
- 질문에 내부 용어를 쓰면 사용자가 추천안을 그냥 고를 수 있다. 이번엔 되물어서 드러났지만, 이전 질문들에서 이해하지 못한 채 추천안을 고른 적은 없었나
- "2달간 G-MOIM에만"이 너무 엄격해서 기간 자체를 건너뛰게 되지 않나

