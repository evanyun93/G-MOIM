# 로드맵 v1

설계 노드 7.1. 결정 #40 #41, 이후 추가 #42~#45. 설계 단계에서 구현 단계로 넘어가는 순서.

작성 2026-09-14 · 전제: 성공 기준 `docs/product/성공기준_v1` (#37~#39)

**날짜를 두지 않는다.** 출시 기한 게이팅은 폐기됐다(#12). 마일스톤마다 **완료 조건**과 **착수 전에 정할 것**만 둔다. 진행 상태는 STATUS가 원본이다.

---

## 1. 결정 요약

| # | 결정 |
|---|---|
| 40 | MVP 검색은 **pg_trgm**. 대상은 킷·기체·별칭. 2자 이하 검색어는 인덱스 없이 `LIMIT` 스캔. 작례 본문 검색은 MVP 제외. 4.5의 "pg_bigm"을 대체 |
| 41 | 구현 순서 **M0 기반 → M1 킷 데이터 → M2 계정 → M3 블로그 → M4 공개 준비**. 킷·기체 페이지·킷 수집·모델러 관계·운영자 작례는 병행. 공개 관문 A의 운영자 작례 조건은 **개수 대신 "M3 완료 후 2달간 운영자가 자기 작례를 G-MOIM에만 올림"** (#37 수정) |
| 42 | 모델러를 초대하지 않는다. 운영자가 첫 모델러 (네이버 블로그 이관 + 신작 + M3 이후 유튜브 작례 채널). 2026-09-14 추가 |
| 43 | B 판정을 공개 후 6개월, 운영자 외 작성자 3명 이상이 작례 2개+로 수정 (성공기준 2장 B) |
| 44 | 운영자 역할은 유저 쿼터 예외. 9GB 차단은 적용 |
| 45 | 운영자 네이버 블로그 이관은 로컬 스크립트, M3 포함. 일반 업로드 API·서버 검증 경유 (5장) |

### #40 근거

- **Neon은 pg_bigm을 지원하지 않는다**(Neon 확장 목록, 2026-09-14 확인). RDS·Cloud SQL은 지원. 4.5를 적을 때 DB 벤더와 대조하지 않았다
- pg_trgm은 Neon·RDS·Cloud SQL 모두 지원해 #19를 충족한다
- pg_trgm은 3글자 단위라 "건담", "자쿠" 같은 **2글자 한국어 검색어는 인덱스 효과가 떨어진다**(구현 시 실측). MVP 검색 대상은 킷·기체·별칭 수천~1만 행 규모라 `LIMIT`을 건 스캔으로 감당 가능하다고 본다
- 작례 본문 검색이 필요해지면 그때 전문 검색 방식을 다시 연다(벤더선택지 2.4)

미확인 — 킷 DB 행 수가 실제로 몇인지(HG 포함 시), 2글자 스캔의 실제 지연. M1 완료 조건에서 잰다.

---

## 2. 마일스톤

### M0 기반

| 항목 | 내용 |
|---|---|
| 범위 | Astro + Workers 골격, `wrangler.jsonc`(`workers_dev: false`), `platform/` 어댑터, 차단 스위치 4종, Drizzle + Neon + Hyperdrive, SQL 마이그레이션, `daily_metrics`, **일 백업 + 다른 Postgres 복구 훈련 1회**, 커스텀 도메인 연결 |
| 착수 전에 정할 것 | **도메인**, **배치 실행 위치**(GitHub Actions / Workers Builds), **백업 저장소·보존 기간**(v3 5장 ⑦: R2에 두면 9GB 계산에서 빠짐), **설계 문서 v4**(결정 #35~#45 반영, 3장에 `comment`·검색 인덱스) |
| 완료 조건 | 빈 페이지가 커스텀 도메인으로 배포됨 · `*.workers.dev` 접근 불가 · 스위치 4종을 켜고 끄는 절차를 실제로 한 번 수행 · 마이그레이션 SQL이 레포에서 적용됨 · 백업 파일을 다른 Postgres에 복구해 행 수 일치 |

복구 훈련을 M0에 둔 이유 — #19 "복구해보지 않은 백업은 없는 것". 데이터가 적을 때 파이프라인을 검증해야 나중에 자동화할 수 있다.

### M1 킷 데이터

| 항목 | 내용 |
|---|---|
| 범위 | `grade_family`·`series`·`unit`·`kit`·`kit_releases`·`kit_units`·`alias`·`slug_history` 스키마, CSV 적재 스크립트(재실행 가능), 검색 API(#40), 속도 제한·`statement_timeout` |
| 착수 전에 정할 것 | 적재 스크립트가 CSV의 `temp_id`를 DB ID·슬러그로 바꾸는 규칙 |
| 완료 조건 | 01b 통과분(MG부터) 적재 · 같은 CSV를 두 번 적재해도 중복 없음 · 2글자 검색 지연 실측 기록 |
| 병행 (Antigravity) | 킷·기체 SSG 페이지. 별도 지시서(03)로. **갤러리 표시 방식은 v3 5장 ②의 판단과 연결되어 M4 전에 바뀔 수 있다고 지시서에 적는다** |

### M2 계정

| 항목 | 내용 |
|---|---|
| 범위 | Better Auth + Drizzle 어댑터, 카카오·네이버·구글, 가입 단계 Turnstile, 소셜 추가 연결, 로그인 island |
| 착수 전에 정할 것 | Better Auth 테이블 이름(v3 5장 ⑫) |
| 완료 조건 | 세 제공자로 가입·로그인 · 같은 이메일 다른 제공자가 자동 병합되지 않음 · **요청당 세션 조회 쿼리 수 실측**(#28) · 캐시된 HTML에 로그인 상태가 섞이지 않음 대조 테스트(#30) |
| 사용자 할 일 | 카카오·네이버·구글 앱 등록. 카카오 비즈 앱 심사는 도메인·개인정보 처리방침 페이지가 생긴 뒤 |

### M3 블로그

| 항목 | 내용 |
|---|---|
| 범위 | Tiptap 에디터 island, 이미지 업로드(브라우저 변환 + 헤더 검증), 쿼터·9GB 차단, 운영자 역할·쿼터 예외(#44), **운영자 네이버 블로그 이관 스크립트(#45)**, 게시 상태·휴지통, 킷 연결, 유튜브 임베드, 작례 SSR + 캐시 + 태그 무효화, 1단 댓글, 고아 이미지 정리 배치, 블로그 스킨 앨범형·목록형 |
| 착수 전에 정할 것 | **사진 하나를 R2와 CDN 캐시에서 실제로 내리는 경로**(v3 5장 ①). 저장 키·캐시 무효화 방식이 업로드 구현과 같이 정해져야 한다 · 댓글 쿼터 초기값 · **이관 규모 실측**(글 수·사진 수 → 예상 저장량과 9GB 차단선, 5장) · 네이버 블로그 사진이 원본 해상도로 받아지는지 |
| 완료 조건 | 방어 체크리스트 "업로드·댓글·작례" 전 항목 · 사진 한 장 변형본 합계 실측(#33 추정치 대체) · 운영자가 실제 작례 1개를 폰과 PC에서 각각 올려봄 · island 요청 수 실측(v3 5장 ③) |
| 이후 | **M3 완료일부터 2달간 운영자 사용 기간 시작**(#41) |

M3가 가장 크다. 지치는 지점이 여기일 가능성이 높다(일지 2026-09-12 예측). 쪼갤 때는 "운영자가 사진 없는 글 → 사진 있는 글 → 킷 연결 → 댓글" 순으로 매 단계가 실제로 쓸 수 있는 상태가 되게 한다.

### M4 공개 준비

| 항목 | 내용 |
|---|---|
| 범위 | 신고 창구 + 임시조치 절차(2.2 설계 포함), 이용약관·개인정보 처리방침·운영정책, Web Analytics + 일 스냅샷 배치(#39), 방어 체크리스트 나머지, noindex 해제 조건 |
| 착수 전에 정할 것 | 2.2 신고·임시조치 설계(v3 5장 ⑤) · **킷·기체 정적 페이지에 비공개·조치된 작례가 최대 하루 남는 문제**(②)의 처리 · 변호사 검토 범위 |
| 완료 조건 | **공개 관문 A 전 항목** (성공기준 2장 A) |

### 공개 → B 판정

공개 후 6개월 판정(#43) — 운영자 외 작성자 3명 이상이 작례 2개 이상. 모델러를 초대하지 않는다(#42).

---

## 3. 병행 트랙

| 트랙 | 누가 | 시작 | 마일스톤과의 접점 |
|---|---|---|---|
| 킷 DB 수집 | Antigravity (01b → RG·PG → HG 목록) | 진행 중 | M1 적재 대상. 관문 A는 MG·RG·PG 검증 + HG 목록 |
| 킷·기체 페이지 | Antigravity (지시서 03, 작성 필요) | M1 스키마 확정 후 | 스타일 토큰(#27)을 먼저 메인 세션이 만든다 |
| ~~5.2 모델러 관계~~ | — | — | **#42로 폐기.** 초대하지 않고 운영자가 첫 모델러 |
| 유튜브 채널 | 사용자 | **M3 이후.** 작례 중심. 개발 이야기는 일지를 바탕으로 나중에 | 5.3 유입 경로. 작례 글에 영상 임베드(#36) |
| 이관 대상 세기 | 사용자 | M3 착수 전 | 네이버 블로그 작례 글 수·글당 사진 수 (5장) |
| 운영자 작례 제작 | 사용자 | 지금 | M3 완료 후 2달 사용 기간(#41) |
| 법무 문서 초안 | 사용자 + 메인 세션 | M2 무렵 | 카카오 비즈 앱 심사, M4 관문 |

---

## 4. v3 5장 검토 항목 배치

| 항목 | 분류 | 언제 |
|---|---|---|
| ① 불변 이미지 URL ↔ 블라인드·임시조치 | **공개 전 필수** | M3 착수 전 (업로드 설계와 함께) |
| ② 정적 페이지 하루 지연 ↔ 비공개·조치 | **공개 전 필수** | M1 지시서에 경고, M4 착수 전 판단 |
| ③ island 요청 2회 | 실측 | M2·M3 완료 조건 |
| ④ MVP 댓글 | 해결 | #38 |
| ⑤ 신고 창구 설계 없음 | **공개 전 필수** | M4 착수 전 |
| ⑥ 신규 일 50장 ↔ 이주 모델러 | 완화 | 운영자 이관은 쿼터 예외(#44). 외부 이주자는 공개 후 실제 사례를 보고 |
| ⑦ 9GB ↔ 백업 저장소 | **M0 착수 전** | 백업 저장소 결정과 함께 |
| ⑧ #13 ISR 문구 | 문서 정리 | 설계 문서 v4 |
| ⑨ #11·#12 v2 누락 | 해결 | v3에서 기록 |
| ⑩ 근거 원문 미기록 | 비차단 | 사용자가 기억나면 보탬 |
| ⑪ 블로그 스킨 범위 | 해결 | 앨범형·목록형 |
| ⑫ Better Auth 테이블 이름 | 구현 시 | M2 착수 전 |
| ⑬ 차별점 표에 카페 없음 | 비차단 | 설계 문서 v4 1장 + B 판정에서 가설 확인 |
| ⑭ 검색 pg_bigm ↔ Neon | 해결 | #40 |

공개 전 필수는 ① ② ⑤ ⑦ 넷.

---

## 5. 운영자 이관 (#42 #44 #45)

2026-09-14 추가. 모델러를 초대하지 않고 운영자가 첫 모델러가 된다. 기존 네이버 블로그 작례(수십~수백 개)를 옮긴다.

| 항목 | 규칙 |
|---|---|
| 방식 | **운영자 전용 로컬 이관 스크립트.** 일반 유저 기능이 아니다 |
| 검증 | 스크립트가 리사이즈·재인코딩(EXIF 제거)을 로컬에서 하고, **일반 업로드와 같은 API·같은 서버 헤더 검증**을 거친다. 검증을 우회하는 R2 직접 쓰기를 만들지 않는다 |
| 본문 | 네이버 글 HTML → Tiptap JSON 변환. 허용 노드(#31)에 없는 요소는 버린다 |
| 쿼터 | 운영자 역할은 유저 쿼터 예외(#44). **전체 9GB 차단은 적용** |
| 킷 연결 | 이관 후 운영자가 확인하며 건다(#10) |
| 원본 글 | 네이버 원본을 남길지·지울지·링크를 걸지는 미정. 같은 글이 두 곳에 있으면 검색엔진이 한쪽만 색인할 수 있다 |

### 저장량 추정 (실측 전)

| 글 수 | 글당 사진 | 사진 한 장 변형본 합계 | 합계 |
|---|---|---|---|
| 50 | 15 | 0.7MB (WebP) | 약 0.5GB |
| 100 | 20 | 0.85MB | 약 1.7GB |
| 300 | 25 | 1MB (JPEG) | **약 7.5GB** |

글 수·사진 수가 많은 쪽이면 **이관만으로 9GB 차단선(#33)에 닿는다.** M3 착수 전 실측 후 아래 중에서 판단한다.
- 이관 대상을 완성작례로 좁힌다
- 이관분에만 가장 큰 변형(2048)을 생략한다
- 9GB 차단선을 올리고 R2 초과 과금(GB당 월 $0.015 수준, 인프라확장플랜)을 감수한다. 차단선은 공격 방어 장치라 없애지 않고 올리기만 한다

구현 시 확인 — 네이버 블로그 사진이 원본 해상도로 받아지는지(표시용으로 축소 저장됐을 가능성), 이관 스크립트가 네이버에 보내는 요청 속도.

## 6. 설계 단계를 닫으며 남는 미정

로드맵에 흡수된 것 — 도메인, 배치 위치, 백업 저장소·보존·복구 주기(M0), 댓글 쿼터(M3), 신고·임시조치(M4).

설계 트리에 남아 있지만 MVP 경로에 없는 것 — 1.2 갤, 1.5.5 자동 태그, 2.1·2.3·2.4 거버넌스, 5.4 이주 이벤트, 6 수익화. B 판정 이후 다시 연다.

## 7. 미판정

- M3를 "매 단계 실제로 쓸 수 있게" 쪼개는 게 1인 개발 속도를 늦추나, 지치지 않게 하나
- ~~5.2 모델러 관계를 코드와 병행한다고 적었지만, 설계·구현에 몰입하면 실제로는 안 할 가능성이 크다~~ → #42로 초대 자체를 하지 않기로 했다
- 운영자 한 명의 작례만 있는 사이트가 방문자에게 커뮤니티로 보이나, 개인 블로그로 보이나(부록 D "빈 방주")
- 2달 운영자 사용 기간 동안 카페에도 같이 올리면 통과인가. "G-MOIM에만"이 너무 엄격하면 기간 자체를 건너뛰게 된다
