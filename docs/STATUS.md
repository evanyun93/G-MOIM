# 설계 진행 현황

**이 파일은 "어디까지 왔나"만 담는다.** "왜 그렇게 정했나"는 `docs/design/` 최신 v 파일에 있다.

작업 시작 전에 이 파일을 먼저 읽는다. 결정이 나면 설계 문서 버전을 올리기 전에 여기부터 갱신한다.

최종 갱신 2026-09-13 · 확정 결정 19건

---

## 지금 하는 일

**노드 `4.1 스택 선정`** — 진행 중.

이걸 정해야 에디터, 인증, 이미지 파이프라인 구현이 구체화된다.

### 4.1의 하위 순서

```
4.1.1 프레임워크          [조건부 확정] Astro. PoC 통과 시 확정 (#17)
4.1.2 재생성 트리거        [확정] 킷·기체 일 1회 배치, 작례 SSR+CDN 캐시 (#18)
4.1.3 DB 접근 계층         ← 다음. 이식성 원칙(#19) 적용
4.1.4 이미지 변환 위치
4.1.5 스타일링
```

**PoC 확인 항목** — Astro Cloudflare 캐시 제공자(실험 단계)에서 CDN 캐시 적중 시 Worker가 호출되지 않는가. 틀리면 작례 요청이 전부 Workers 일 10만 한도를 소모하므로 #18을 재검토한다.

**전제 정정 (2026-09-13 확인)** — "Pages 빌드 월 500회"는 Pages Git 연동에만 해당해 병목이 아니다. 정적 에셋 요청은 무료·무제한이고 Worker를 호출하지 않는다. 대신 배포당 정적 파일 2만 개(무료) 한도가 있어 작례를 SSG로 만들 수 없다.

### 4.1 이후 순서

```
1.2.2 글·댓글·추천 모델
1.1.3 에디터 / 이미지 업로드
4.6 인증 / 계정
0.3.2 타겟 유저 성향
0.4 성공 기준
7.1 로드맵
```

---

## 설계 트리

`[확정]` `[진행]` `[미정]` `[보류]` `[후속]`

```
0. 정체성 / 전제
   0.1 프로젝트 목적과 우선순위          [확정] 포트폴리오·재미 → 수익화
   0.2 네이밍 / 브랜딩                  [진행] G-MOIM 확정, 로고·톤 미정
   0.3 타겟 유저
       0.3.1 수록 범위                  [확정] #4
       0.3.2 유저 성향                  [미정]
   0.4 성공 기준                        [미정]

1. 제품
   1.1 작례 아카이브 (개인 블로그)
       1.1.1 작례 데이터 스키마          [확정] #9
       1.1.2 개인 카테고리 vs 시스템 태그 [확정] category 자유 / technique 시스템
       1.1.3 에디터 / 이미지 업로드       [미정]
       1.1.4 네이버 블로그 이관           [보류] 핫링크 차단 → 재업로드 필요
       1.1.5 블로그 스킨                  [보류] MVP는 목록형 하나만
   1.2 커뮤니티 (갤)                     MVP 제외
       1.2.1 게시판 구조                 [진행] 디시 말머리 차용
       1.2.2 글·댓글·추천 모델            [미정] 추천→개념글이 유입 엔진
       1.2.3 유저 신뢰도 / 등급           [미정]
   1.3 차별점과 메리트                   [확정] #1 #2 #3
   1.4 MVP 범위                         [확정] #11
   1.5 킷 DB
       1.5.1 수록 범위                   [확정] #4
       1.5.2 데이터 스키마                [확정] #6 #7 #8 #15 #16
       1.5.3 데이터 소싱                  [확정] #5
       1.5.4 킷 ↔ 작례 연결               [확정] #10
       1.5.5 자동 태그 제안               [후속] MVP 이후

2. 운영 / 거버넌스                       MVP 제외 (신고 창구만 포함)
   2.1 AI 모더레이션 계층                [진행] 초안 있음
   2.2 신고 · 임시조치 워크플로우          [진행] 요건 파악, 설계 미정
   2.3 유저 관리자 제도                   [미정]
   2.4 이의신청 / 분쟁 처리               [미정]

3. 법무 / 컴플라이언스                   [진행] 조사 완료. MVP는 최소 요건만

4. 기술 아키텍처
   4.1 스택 선정                         [진행] Astro 조건부, 갱신 방식 확정  ← 현재 작업
   4.2 이미지 파이프라인                  [진행] 방향 확정, 구현 미정
   4.3 스토리지 / CDN / 비용              [진행] Cloudflare R2 + Neon 방향
   4.4 DB 설계                           [진행] 설계문서 3장이 초안
   4.5 검색                              [진행] Postgres + pg_bigm + alias
   4.6 인증 / 계정                        [미정] 벤더 잠금 주의

5. 성장 / 콜드스타트
   5.1 방주 전략                          [진행] 빈 방주 금지
   5.2 초기 모델러 관계 구축                [미정] 대체 불가 자산
   5.3 유입 경로                          [진행] SEO는 후행 지표
   5.4 이주 이벤트 대응                     [미정]

6. 수익화                                [보류] 전부

7. 실행
   7.1 로드맵                            [미정]
   7.2 현재 상태 추적                     [확정] 이 파일
   7.3 운영 모니터링 페이지                [확정] #14. MVP는 집계 테이블만
```

---

## 확정 결정

근거와 부대 조건은 `docs/design/` 최신 v 파일에 있다. 여기는 목록만.

| # | 요약 | 반영 |
|---|---|---|
| 1 | 갤 글과 블로그 포스트는 별개 객체. 블로그에 쓰고 갤에 공유 | v1 |
| 2 | 갤 공유는 링크 카드. 댓글은 블로그로 일원화 | v1 |
| 3 | 킷 DB가 첫 진입 가치이자 데이터 뼈대 | v1 |
| 4 | 범위는 건프라 한정. 전 등급, 구판 포함 | v1 |
| 5 | 위키 기반 전 등급 얕게 수집 + 점진 보완. 대표 이미지는 유저 작례 | v1 |
| 6 | 킷 ↔ 기체 M:N (`kit_units`) | v2 |
| 7 | 재판은 `kit_releases`로, 외형 변형은 `base_kit_id` 연결 | v2 |
| 8 | 등급은 `grade_family` 단일 계층. MGSD 독립 | v2 |
| 9 | 포스트가 곧 작례 단위. `post_type`으로 구분 | v2 |
| 10 | 킷 연결은 선택. 자동 태그 제안은 후속 | v2 |
| 11 | MVP = 킷 DB + 블로그 + 계정. 갤 제외 | v2 |
| 12 | ~~출시를 기한으로 게이팅~~ **폐기.** 배포는 상시, 공개만 별도 판단 | v2 |
| 13 | 페이지 성격별 렌더링 분리 (SSG/ISR/SSR/CSR) | **미반영** |
| 14 | 운영 모니터링 자체 구축. MVP는 `daily_metrics` 집계만 | **미반영** |
| 15 | `parent_kit_temp_id` 신설. 확장 파츠 ↔ 원본 킷 연결 | **미반영** |
| 16 | `limited_channel`은 필수급. MG 358종 중 143종이 한정 유통 | **미반영** |
| 17 | 프레임워크는 Astro. 캐시 동작 PoC 통과 조건 | **미반영** |
| 18 | 킷·기체는 SSG + 일 1회 배치 재빌드. 작례는 SSR + CDN 캐시(태그 무효화). #13의 작례 방식 구체화 | **미반영** |
| 19 | 이식성 원칙 — 모든 인프라는 다른 플랫폼으로 옮길 수 있게. DB는 벤더 밖 자동 백업 + 복구 훈련 | **미반영** |

**#13~#19는 설계 문서에 아직 안 들어갔다.** v3 작성 시 반영한다.
**#7은 수정됐다** — `kit_releases`를 MVP 수집 범위에 포함. 스키마에 자리가 없으니 초판·재판 정보가 한 행에 섞여 들어왔다.

---

## 병행 작업

### 킷 카탈로그 수집 — 브랜치 `data/kit-db`

Antigravity 전담. 워크트리 `C:\dev\G-MOIM-kit-db`. 지시서는 `docs/tasks/01a_킷DB병행작업_antigravity.md`.
킷·기체 페이지 구현도 Antigravity 담당이나 4.1 확정 후 별도 지시서로 착수한다.

| 단계 | 상태 |
|---|---|
| MG 목록 (4-1) | 신규 작성 중. 이전 358종 목록은 파일이 없어 참고치로만 사용 |
| MG CSV (4-2) | 미착수 |
| RG 이하 | 미착수 |

**재지시 반영** — 이전 보고의 "2022년 일반 유통 MG 0건"은 소스 접근 실패를 "없음"으로 보고한 것으로 판단. 연도×채널별 `확인`/`부재확인`/`확인불가` 출력 계약을 01a에 명시했다.

**폐기된 결과물** — 1·2차 수집분(kits_1차/2차)은 v1 지시서 기준이라 스키마가 다르다. 대조용으로만 쓴다.

---

## 문서 상태

| 문서 | 최신 | 갱신 필요 |
|---|---|---|
| `docs/design/` | v2 | 결정 #13~#19 반영해 v3 |
| `docs/journal/` | 2026-09-13 | — |
| `docs/infra/` | v1 | 템플릿과 중복분 제거. #19 반영해 이전 런북 추가 |
| `docs/tasks/` | 01_v2, 01a, 02 | — |

`docs/tasks/01_킷DB수집.md`는 폐기된 v1이다. 이 파일을 기준으로 작업하지 않는다.

---

## 갱신 규칙

- 결정이 나면 **여기부터** 갱신한다. 설계 문서 버전은 5건 이상 쌓였을 때 올린다
- 결정 표에 추가할 때 `반영` 열을 `미반영`으로 두고, v3 작성 시 일괄 갱신한다
- 트리 노드 상태를 함께 바꾼다
- 이 파일은 짧게 유지한다. 근거·논의·대안은 설계 문서로 보낸다
