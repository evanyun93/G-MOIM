# G-MOIM 아키텍처 v1

인프라 구성과 코드 구조를 그림으로 남긴다. 검토·수정할 때 이 문서부터 본다.

작성 2026-09-13 · 갱신 2026-09-14 · 기준 결정 #13, #17~#34, #39

---

## 읽는 법

| 표시 | 뜻 |
|---|---|
| 실선 상자·화살표 | 확정된 결정에 근거 |
| 점선 상자·화살표 | **미정.** 해당 설계 노드에서 결정 예정 |
| `[초안]` | 결정에서 끌어낸 제안. 구현 시작 전에 검토 |

**이 문서는 결정을 만들지 않는다.** 결정은 `docs/STATUS.md`에 먼저 기록하고, 여기는 그림으로 옮긴다. 둘이 어긋나면 STATUS가 맞다.

**갱신 규칙** — 인프라 구성요소나 코드 계층이 바뀌는 결정이 나면 이 문서를 고친다. 구조가 크게 바뀌면 v2 파일을 새로 만든다.

---

## 1. 인프라 구성도

```mermaid
flowchart LR
    U["방문자 / 유저"]
    BOT["봇 · 공격 트래픽"]

    subgraph CF["Cloudflare"]
        direction TB
        subgraph EDGE["엣지 방어 (커스텀 도메인)"]
            WAF["WAF 속도 제한 규칙<br/>Bot Fight Mode<br/>AI 크롤러 차단"]
        end
        SA["Static Assets<br/>킷·기체 페이지 HTML<br/>무료·무제한, Worker 미호출"]
        WC["Workers Cache<br/>작례·홈 캐시<br/>적중해도 요청 수 차감"]
        W["Worker<br/>Astro SSR"]
        RL["Rate Limiting 바인딩<br/>IP 단위 폭주"]
        HD["Hyperdrive<br/>연결 풀<br/>무료 일 10만 쿼리"]
        R2["R2<br/>이미지 저장<br/>S3 호환 API"]
        IMG["이미지 도메인<br/>CDN 캐시<br/>쿼리스트링 무시"]
    end

    subgraph NEON["Neon"]
        PG[("Postgres<br/>표준 기능만")]
    end

    subgraph CI["배치 (실행 위치 미정)"]
        BUILD["일 1회 빌드·배포<br/>킷·기체 SSG"]
        DUMP["일 1회 pg_dump"]
    end

    OAUTH["카카오 · 네이버 · 구글<br/>소셜 로그인 (#29)"]

    BK[("백업 저장소<br/>DB 벤더 밖<br/>위치 미정")]
    REPO["GitHub 레포<br/>코드 · 마이그레이션 SQL<br/>킷 카탈로그 CSV"]

    U --> WAF
    BOT --> WAF
    WAF -->|정적 경로| SA
    WAF -->|동적 경로| WC
    WC -->|캐시 미스| W
    W --> RL
    W --> HD --> PG
    W -->|"업로드 (헤더 검증 후 쓰기)"| R2
    U -->|이미지| IMG --> R2
    W <-->|"OAuth (Better Auth, 세션은 Postgres)"| OAUTH

    REPO --> BUILD
    BUILD -->|빌드 시 직접 연결| PG
    BUILD -->|배포| SA
    PG -.-> DUMP -.-> BK

    classDef undecided stroke-dasharray: 5 5
    class CI,BUILD,DUMP,BK undecided
```

### 구성요소 표

| 구성요소 | 역할 | 근거 결정 | 무료 한도 / 비용 주의 |
|---|---|---|---|
| WAF · Bot Fight Mode | 엣지에서 봇·폭주 차단 | #20 | 속도 제한 규칙 무료 1개 |
| Static Assets | 킷·기체 정적 페이지 | #13, #18 | 파일 2만 개/배포 |
| Workers Cache | 작례·홈 SSR 결과 캐시, 태그 무효화 | #18 | **적중도 요청 수 차감** |
| Worker (Astro) | SSR, API | #17 | 일 10만 요청, 요청당 CPU 10ms |
| Rate Limiting 바인딩 | IP 단위 초 단위 제한 | #23 | 부정확 (위치별 카운트) |
| Hyperdrive | Worker → Postgres 연결 풀 | #22 | 일 10만 쿼리 |
| Neon Postgres | 핵심 데이터 | 부록 C, #19 | 0.5GB, 월 100 CU-hour. 초과 시 정지(무과금) |
| R2 | 이미지 원본 대신 변환본 저장 | 부록 C | **무료 플랜에서도 초과분 과금** |
| 배치 | 일 1회 SSG 재빌드, DB 백업 | #18, #19 | 실행 위치 미정 (GitHub Actions 또는 Workers Builds) |
| 백업 저장소 | DB 덤프 보관 | #19 | 위치 미정. **Neon 밖**이어야 함 |
| Better Auth (라이브러리) | 소셜 로그인, 세션. 테이블은 우리 Postgres | #28, #29 | 세션 조회가 요청마다 쿼리를 내면 Hyperdrive 한도 소모 |

---

## 2. 페이지별 요청 흐름

```mermaid
flowchart TD
    REQ["요청"] --> EDGE{"엣지<br/>WAF · 봇 차단"}
    EDGE -->|차단| X1["403 / 챌린지"]
    EDGE -->|통과| KIND{"경로"}

    KIND -->|"/kit/* /unit/*"| SA["Static Assets<br/>SSG HTML 반환"]
    KIND -->|"/post/* /"| CACHE{"Workers Cache<br/>적중?"}
    KIND -->|"쓰기 API"| GUARD
    KIND -->|"글쓰기·관리"| CSR["정적 셸 + CSR"]

    CACHE -->|적중| HIT["캐시 응답<br/>요청 수 차감, CPU 0"]
    CACHE -->|미스| SSR["Worker SSR"]
    SSR --> Q1["Hyperdrive → Postgres<br/>LIMIT · statement_timeout"]
    Q1 --> SET["캐시 저장<br/>태그: post-id, path<br/>로그인 상태 미포함 (#30)"]

    CSR -.->|API 호출| GUARD

    subgraph GUARD["쓰기 API 가드 (순서대로)"]
        direction TB
        G1["차단 스위치 확인<br/>READ_ONLY 등"] --> G2["IP 속도 제한<br/>Rate Limiting 바인딩"]
        G2 --> G3["인증<br/>Better Auth 세션"] --> G4["유저 쿼터<br/>DB 테이블"]
        G4 --> G5["입력 검증<br/>크기 · 타입<br/>이미지는 헤더 검증 (4.5)"]
    end

    G5 --> WRITE["DB 쓰기 / R2 업로드"]
    WRITE --> PURGE["캐시 태그 무효화"]

    G1 -->|꺼짐| X2["503"]
    G2 -->|초과| X3["429"]
    G3 -->|실패| X4["401"]
    G4 -->|초과| X5["429"]
    G5 -->|위반| X6["400 / 413"]
```

**가드 순서의 이유** — 싼 검사를 먼저 한다. 스위치 확인과 IP 제한은 DB에 닿지 않는다. 쿼터 확인부터 DB를 쓰므로 인증된 요청만 도달하게 한다. 크기·타입 검증은 스토리지 쓰기보다 반드시 앞(#20).

---

## 3. 배치 파이프라인

```mermaid
flowchart LR
    subgraph DAILY["매일 (스케줄 전용, 공개 트리거 없음)"]
        direction TB
        B1["레포 체크아웃"] --> B2["astro build<br/>킷·기체 SSG<br/>DB 직접 연결로 읽기"]
        B2 --> B3["wrangler deploy"]
        D1["pg_dump"] --> D2["DB 벤더 밖 저장소에 업로드<br/>보존 기간 미정"]
        C1["고아 이미지 표시<br/>미완료·미연결 24시간 경과<br/>→ deleted"] --> C2["유예 30일 지난 파일<br/>R2에서 실제 삭제"]
        M1["Web Analytics 전날 조회 수치 읽기<br/>(어댑터 뒤)"] --> M2["daily_metrics에 저장 (#39)"]
    end

    subgraph PERIODIC["정기 (주기 미정)"]
        direction TB
        R1["최신 덤프 다운로드"] --> R2["별도 Postgres에 복구"]
        R2 --> R3["테이블별 행 수 대조"]
        R3 --> R4["결과 기록 / 실패 시 알림"]
    end

    MIG["마이그레이션 적용<br/>레포 SQL 파일 → DB<br/>수동 또는 배포 파이프라인"]

    D2 -.-> R1

    classDef undecided stroke-dasharray: 5 5
    class PERIODIC,R1,R2,R3,R4 undecided
```

- 빌드는 Hyperdrive가 아니라 **DB 직접 연결**로 읽는다. Hyperdrive는 Worker 런타임용이다
- 킷 페이지가 늘어나도 빌드는 하루 1회라 빌드 한도(Workers Builds 월 3,000분 등)는 병목이 아니다
- 복구 훈련은 #19의 필수 요건. 자동화 방식은 미정
- 고아 이미지 정리는 #25. 상세는 4.4
- 조회 수치 스냅샷은 #39. 정적 파일·캐시 적중 조회는 우리 코드가 셀 수 없어 벤더 수치를 옮겨 적는다. 표본 추정치

---

## 4. 코드 아키텍처 `[초안]`

### 4.1 계층

```mermaid
flowchart TD
    subgraph WEB["웹 계층 (Astro)"]
        PAGES["pages/<br/>킷·기체: prerender<br/>작례·홈: SSR + cache.set"]
        API["pages/api/<br/>쓰기 API"]
        MW["middleware<br/>가드 체인"]
        ISL["islands/<br/>글쓰기 에디터 등 CSR"]
    end

    subgraph DOMAIN["도메인 계층 (플랫폼 무관)"]
        SVC["services/<br/>kit · unit · post · user · quota"]
    end

    subgraph DATA["데이터 계층"]
        SCHEMA["db/schema.ts<br/>Drizzle 스키마"]
        REPOS["db/queries/<br/>LIMIT 강제"]
        MIGR["db/migrations/*.sql<br/>생성 후 커밋, 원본"]
    end

    subgraph ADAPTER["플랫폼 어댑터 (Cloudflare 접근은 여기서만)"]
        ENV["platform/env.ts<br/>환경 변수 · 차단 스위치"]
        CACHEP["platform/cache.ts<br/>캐시 태그 무효화"]
        RATE["platform/rate-limit.ts<br/>Rate Limiting 바인딩"]
        STORE["platform/storage.ts<br/>S3 호환 클라이언트"]
        DBC["platform/db.ts<br/>Hyperdrive 연결 문자열"]
    end

    PAGES --> SVC
    API --> MW --> SVC
    ISL -.->|fetch| API
    SVC --> REPOS --> SCHEMA
    SCHEMA -.->|drizzle-kit generate| MIGR

    MW --> ENV
    MW --> RATE
    SVC --> CACHEP
    SVC --> STORE
    REPOS --> DBC

    classDef adapter fill:#fff3cd,stroke:#b8860b
    class ENV,CACHEP,RATE,STORE,DBC adapter
```

### 4.2 의존 규칙

| 규칙 | 근거 |
|---|---|
| `platform/` 밖에서 `cloudflare:*` 모듈, 바인딩(`env.*`)을 직접 쓰지 않는다 | #19 |
| `services/`는 Astro·Cloudflare를 모른다. 순수 TypeScript | #19 |
| DB 접근은 `db/queries/`를 통한다. 목록 쿼리는 `LIMIT` 필수 | #20 |
| 스키마 변경은 `drizzle-kit generate` → SQL 파일 커밋 → 적용. `drizzle-kit push` 금지 | #21 |
| 쓰기 API는 전부 `middleware` 가드 체인을 통과한다 | #20 |
| IP 단위 제한은 `platform/rate-limit.ts`, 유저 쿼터는 `services/quota` + DB | #23 |

규칙 위반은 나중에 린트(import 제한)로 막는 것을 검토한다.

### 4.3 디렉터리 `[초안]`

```text
G-MOIM/
├── src/
│   ├── pages/            라우트. 킷·기체 prerender, 작례 SSR
│   │   └── api/          쓰기 API
│   ├── middleware.ts     가드 체인 (스위치 → IP 제한 → 인증 → 쿼터 → 검증)
│   ├── islands/          CSR 컴포넌트. 에디터는 React + Tiptap (#31)
│   ├── styles/           Tailwind 진입점 + 토큰(CSS 변수). 스킨은 토큰 교체 (#27)
│   ├── services/         도메인 로직. 플랫폼 무관
│   ├── db/
│   │   ├── schema.ts     Drizzle 스키마
│   │   ├── queries/      쿼리 함수
│   │   └── migrations/   생성된 SQL. 원본
│   └── platform/         Cloudflare 접근 유일 지점
├── scripts/              배치: 백업, 복구 훈련, 카탈로그 적재
├── data/                 킷 카탈로그 CSV (기존)
├── docs/                 (기존)
├── astro.config.mjs
├── drizzle.config.ts
└── wrangler.jsonc        workers_dev: false, cache.enabled, limits
```

### 4.4 이미지 데이터 모델과 수명 (결정 #24, #25)

사진이 많은 서비스라 이미지를 **게시물에 종속시키지 않고 유저 소유 자산**으로 둔다. 파일은 R2, DB에는 메타데이터만.

```mermaid
erDiagram
    users ||--o{ image : "소유"
    users ||--o{ post : "작성"
    post ||--o{ post_images : ""
    image ||--o{ post_images : "참조됨"
    post |o--o| image : "cover_image_id"

    image {
        id id PK
        id user_id FK "쿼터 집계"
        text content_hash "SHA-256, UNIQUE(user_id, content_hash)"
        text storage_key "해시 기반 불변 키. 변형본은 키/2048·1024·400 (#32)"
        int byte_size "쿼터 집계. 변형본 합계 (#26)"
        int width
        int height
        text format "webp / jpeg (#32)"
        text status "pending / ready / deleted"
        text moderation_status "unchecked / ok / blinded"
        timestamp created_at "INDEX(user_id, created_at)"
        timestamp unlinked_at "연결 없어진 시각. 24시간 정리 기준"
        timestamp deleted_at
    }
    post_images {
        id post_id PK
        id image_id PK
        int sort_order
        text caption
    }
    post {
        id id PK
        id user_id FK
        id cover_image_id FK
        jsonb body "Tiptap JSON (#31)"
        text status "draft / published / private (#34)"
        timestamp deleted_at "휴지통. 30일 후 영구 삭제 (#34)"
    }
```

갤(MVP 제외)이 생기면 `gal_post_images`가 같은 `image`를 참조한다. 복제하지 않는다.

게시 상태 (#34):

| 상태 | 누가 보나 | 캐시 |
|---|---|---|
| `draft` | 본인. 임시저장도 draft 글로 (#25) | 안 함 |
| `published` | 전체 | Workers Cache |
| `private` | 본인 | **안 함.** 캐시 경로를 타지 않는다 (#30) |
| `deleted_at` 있음 | 본인 휴지통. 30일 후 영구 삭제 → 사진은 연결 해제되어 #25 정리 흐름으로 | 캐시 무효화 |

비공개 글의 사진 URL은 **비밀이 아니다.** 해시 키라 추측은 불가능하지만, URL을 받은 사람은 볼 수 있다. 비공개 설정 화면에 이 사실을 안내한다.

**본문과 `post_images` 동기화** (#31) — 저장 시 서버가 본문 JSON의 이미지 노드에서 `image_id`를 뽑아 `post_images`를 다시 맞춘다. 이미지 노드의 `image_id`는 요청한 유저 소유인지 확인한다. 본문 HTML 렌더는 허용한 노드·마크만 변환하므로 별도 XSS 정화를 두지 않는다.

#### 수명 주기

```mermaid
stateDiagram-v2
    [*] --> pending: 업로드 시작
    pending --> ready: 검증·변환·저장 완료
    pending --> deleted: 24시간 내 미완료

    ready --> attached: 글(초안 포함)에 연결
    ready --> deleted: 24시간 내 미연결
    attached --> ready: 글에서 빠짐 / 글 삭제

    deleted --> ready: 30일 내 복구
    deleted --> [*]: 30일 경과 → R2 파일 삭제

    note right of attached
        attached는 별도 컬럼이 아니다.
        status = ready 이면서
        post_images 행이 있는 상태
    end note
```

| 규칙 | 이유 |
|---|---|
| 업로드는 `pending` 행을 먼저 만들고 시작 | 쿼터를 업로드 전에 확인하고, 실패한 업로드도 추적 |
| 미연결 24시간이면 삭제 표시 | 업로드만 하고 글을 안 쓰는 저장소 채우기 차단 (#20) |
| **임시저장은 반드시 draft 글로 서버에 저장** | 브라우저에만 임시저장하면 24시간 뒤 사진이 지워진다 |
| 글에서 빠진 사진은 `unlinked_at`을 기록해 24시간 시계 시작. 다시 연결되면 `unlinked_at = null` | 수정 중 사진을 뺐다가 되돌리는 경우를 흡수. 업로드 시각(`created_at`)으로는 판단 불가 |
| 정리 대상 조건: `status=ready` AND 연결 0건 AND `unlinked_at < now - 24h` | 업로드 직후에도 `unlinked_at`을 기록해 같은 조건으로 처리 |
| 파일 실제 삭제는 30일 유예 후 | 실수·오판 복구 |
| 같은 유저가 같은 파일 재업로드 → 기존 행 재사용 | 반복 업로드로 쿼터·저장소 소모 차단 |
| 유저 간 중복 제거는 하지 않음 | 삭제 시 참조 관리가 복잡해지는 데 비해 절약이 작다 |

### 4.5 이미지 업로드 흐름 (결정 #26)

변환은 **브라우저**가 하고, 서버는 **믿지 않고 검증만** 한다.

```mermaid
sequenceDiagram
    actor U as 유저
    participant B as 브라우저 (에디터)
    participant W as Worker (가드 체인)
    participant DB as Postgres
    participant R2 as R2

    U->>B: 사진 선택
    B->>B: 장변 2048 / 1024 / 400 리사이즈<br/>재인코딩(WebP, 불가 시 JPEG) → EXIF 제거
    B->>B: 콘텐츠 해시 계산

    B->>W: 업로드 요청 (해시, 용량 합계)
    W->>W: 차단 스위치 · IP 제한 · 인증
    W->>DB: 유저 쿼터 + 전체 저장량 확인 (#33)
    alt 같은 해시가 이미 있음
        DB-->>W: 기존 image
        W-->>B: 기존 image 재사용
    else 유저 쿼터 초과
        W-->>B: 429
    else 전체 저장량 9GB 도달
        W-->>B: 503 업로드 일시 중지
    else 통과
        W->>DB: image 행 생성 (status = pending)
        W-->>B: image_id

        B->>W: 파일 전송 (본문)
        W->>W: 헤더 검증<br/>매직 바이트 포맷 · 해상도 ≤ 2048<br/>EXIF/XMP 없음 · 용량 · 해시 일치
        alt 검증 실패
            W->>DB: status = deleted
            W-->>B: 400 / 413
        else 검증 통과
            W->>R2: 저장 (해시 기반 불변 키)
            W->>DB: status = ready, unlinked_at = now
            W-->>B: 완료
        end
    end

    U->>B: 글 저장 (초안 포함)
    B->>W: post + image_id 목록
    W->>DB: post_images 연결, unlinked_at = null
```

| 규칙 | 이유 |
|---|---|
| 서버는 **디코딩하지 않고 헤더만** 읽는다 | 무료 Worker CPU 10ms·메모리 128MB 안에서 가능한 유일한 방식 |
| 검증은 R2 쓰기 **전** | 규칙 위반 파일이 한 번도 저장되지 않게 (#20) |
| R2 직접 서명 URL 업로드 경로를 만들지 않는다 | 검증을 우회하는 경로가 생긴다 |
| 해시는 서버가 받은 바이트로 다시 계산해 대조 | 클라이언트가 보낸 해시를 믿지 않는다 |
| 변형본도 같은 검증을 통과해야 한다 | 썸네일 자리에 원본을 끼워 넣는 우회 차단 |

#### 포맷·프리셋·쿼터 (#32, #33)

| 항목 | 값 |
|---|---|
| 파일 선택 허용 | `accept="image/jpeg,image/png,image/webp"` — 아이폰은 이 설정에서 HEIC를 JPEG로 자동 변환해 넘긴다 |
| 출력 포맷 | 캔버스로 WebP 인코딩 → 결과 `blob.type`이 WebP가 아니면(Safari) JPEG로 다시 인코딩. 서버는 WebP·JPEG만 받는다 |
| HEIC | 브라우저가 못 여는 경우(윈도우 Chrome 등) 거부 + JPEG 변환 안내. 거부 건수를 `daily_metrics`에 집계 |
| 변형 프리셋 | 장변 2048(크게 보기) / 1024(본문) / 400(목록·카드). 비율 유지, 원본보다 크게 만들지 않는다 |
| 변형본 크기 상한 | 2048: 3MB · 1024: 800KB · 400: 150KB |
| 유저 쿼터 | 신규(가입 7일 미만) 일 50장·총 300MB / 일반 일 200장·총 2GB / 글당 50장 |
| 전체 저장량 차단 | R2 총량(`image.byte_size` 합계, 삭제 전 포함) 9GB 도달 시 업로드 자동 중지. R2 무료 10GB 초과 과금 방지 |

수치는 초기값이다. 실측(사진 한 장 변형본 합계, 추정 WebP 0.7MB·JPEG 1MB) 후 조정하고, 값은 코드 한 곳(`services/quota`)에 모은다.

**보조 경로** — 화질·HEIC·브라우저 호환 문제가 실제로 생기면 Cloudflare Images 서버 변환을 `platform/` 어댑터 뒤에 보조로 붙인다. 무료 월 5,000회 한도라 기본 경로로 쓰지 않는다.

---

## 5. 이식성 교체 지점

"이 구성요소가 막히면 무엇을 바꾸나." #19.

| 구성요소 | 현재 | 막혔을 때 바꿀 곳 | 대안 예시 | 데이터 이관 |
|---|---|---|---|---|
| DB | Neon | `platform/db.ts` 연결 문자열 | Supabase, RDS, 자체 Postgres | **벤더 밖 백업**에서 복구 |
| DB 연결 풀 | Hyperdrive | `platform/db.ts` | 표준 드라이버 직접 연결, PgBouncer | 없음 |
| 컴퓨트 | Workers | Astro 어댑터 교체, `platform/` 전체 | Node 어댑터 + VPS, Vercel, Netlify | 없음 |
| 정적 호스팅 | Static Assets | 배포 대상 | 아무 정적 호스팅 + CDN | 빌드 산출물 재배포 |
| SSR 캐시 | Workers Cache | `platform/cache.ts` | CDN 캐시 헤더, Varnish | 없음 (재생성) |
| IP 속도 제한 | Rate Limiting 바인딩 | `platform/rate-limit.ts` | 리버스 프록시 제한, Redis | 없음 |
| 이미지 | R2 | `platform/storage.ts` 엔드포인트·키 | S3, B2, MinIO | S3 호환 동기화 (용량에 비례해 시간 소요, 미계산) |
| 엣지 방어 | Cloudflare WAF | DNS·프록시 설정 | 다른 CDN WAF | 없음. 규칙 재작성 |
| 스키마 | Drizzle | 쿼리 코드 | Kysely, 생SQL | 없음. SQL 마이그레이션 파일이 원본 |

---

## 6. 미정 항목

그림에서 점선으로 표시한 부분. 결정되면 이 문서를 갱신한다.

| 항목 | 설계 노드 | 영향 받는 그림 |
|---|---|---|
| 배치 실행 위치 (GitHub Actions / Workers Builds) | 미배정 | 1, 3 |
| DB 백업 저장소와 보존 기간 | 미배정 (#19) | 1, 3 |
| 복구 훈련 주기와 자동화 | 미배정 (#19) | 3 |
| 커스텀 도메인 | 인프라 7장 | 1 |
